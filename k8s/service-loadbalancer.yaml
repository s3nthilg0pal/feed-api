apiVersion: v1
kind: Service
metadata:
  name: feedapi
  labels:
    app: feedapi
spec:
  type: LoadBalancer
  selector:
    app: feedapi
  ports:
    - name: http
      port: 80
      targetPort: 8080
